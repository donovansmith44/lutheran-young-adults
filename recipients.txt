# age public keys that can decrypt this project's secrets.
# Each collaborator generates their own keypair, sends their public key,
# and it gets added here. Private keys NEVER appear in this file.
#
# To add a collaborator (e.g., Donny):
#   1. They run:  age-keygen -o ~/.config/event_planner/key.txt
#   2. They send you the single line starting with "age1..." from that file
#   3. Paste it below with their name in a comment
#   4. Re-encrypt existing secrets:  python3 secret.py rekey
#   5. Commit this file.
#
# To remove a collaborator:
#   1. Delete their line below
#   2. python3 secret.py rekey   (their old copy is still decryptable
#      offline with their private key, so rotate any leaked-risk secrets)
#   3. Commit.

# mlaine1 (Martti, laptop)
age1aewj3d6xdyffe3vksj37m5q6f8wltkajzga4vtft2vmqh92ew9yslqa26j
